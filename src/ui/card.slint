// ── Card ─────────────────────────────────────────────────────────────────────
export component Card inherits Rectangle {
    border-radius: 8px;
    background: @linear-gradient(150deg, #12181f 0%, #0c1014 100%);
    border-width: 1px;
    border-color: #ffffff0c;
}